import { createTheme } from "@mui/material/styles";
export const theme = createTheme({
  palette: {
    mode: "light",
    primary: {
      main: "#025959",
    },
    secondary: {
      main: "#04adbf",
    },
    background: {
      default: "#f2e0c9",
    },
  },
});
